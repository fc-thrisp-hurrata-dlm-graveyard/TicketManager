= simple_form_for(TicketComment.new, :url => ticket_comments_path) do |f|
    = f.input :body, :required=>:false, :label=>false, :as=>:text
    = f.hidden_field :ticket_id, :value => @ticket.id
    = f.button :submit
